name: "On Pull Request"

on:
  pull_request:
    branches: [ "main" ]

jobs:
  hello-cicd:
    runs-on: ubuntu-latest
    name: Hello CI/CD

    steps:
    - name: Hello World
      run: |
        echo "Hello World from Pull Request"
